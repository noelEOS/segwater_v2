[pytest]
# Run from the repo root so `import src...` resolves.
# `scripts/tests` holds the self-contained script harnesses (backup, stitcher
# device parity); they are real coverage and must not be collected only by hand.
testpaths = tests scripts/tests
python_files = test_*.py
addopts = -v
